return {
   "stevearc/dressing.nvim",
   opts = {
      input = {
         win_options = {
            winblend = 0, -- don't try to make window transparent
            winhighlight = "NormalFloat:Normal,FloatBorder:MoreMsg",
         },
      },
   },
}
