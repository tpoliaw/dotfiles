--type-add
prop:*.properties

--type-add
launch:*.launch

--type-add
osgi:{MANIFEST.MF, plugin.xml, feature.xml}

--type-add
eclipse:{*.prefs,.classpath,.project,build.properties}

--type-clear
java

--type-add
java:*.java

--type-add
zlog:*log.gz

--type-add
git:.gitignore

--type-add
rcp:{*.product,*.feature}

--hidden

--glob
!**/.git

--no-heading

--smart-case

--glob-case-insensitive
